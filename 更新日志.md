#### 5/??? 所有接口完成测试可以正常使用了

#### 5/29 加入了使用langchian框架做的测试agent,虽然它没什么用

#### 6/13 

1. posts模型中token不再从前端获取而是后端直接传，新加了置顶判断

2. user模块中的路由函数，全部新增本人与管理员认证

3. crud/posts函数中，引用了or_模块来根据用户显示帖子

   优化了筛选帖子的代码，新增了板块筛选

4. 新增了帖子板块orm模型，并为posts的orm模型新增外键关联至板块id

5. 新增板块创建与更新的pydantic模型

6. 新增了板块(category)的crud函数模块，内有获取、通过id获取、新增、更新、删除模块

   等逻辑，并应用到路由

7. 更新挂载的路由

8. 评论orm模型中，评论新增父评论关联，并添加作者、父评论、依赖映射，同时用户orm模型

   中也关联了自己的所有评论

   pydantic模型中也新增父评论id的写入

   评论的crud模块中，增加父评论校验，通过帖子id返回评论改为只返回一级评论即父评论

9. 新增了点赞、收藏、通知等功能，并完善模型、crud函数、路由函数等内容

​		

